function [] = ui_boat_callback(hObject, eventdata, boat)

% answer = inputdlg(prompt,dlg_title,num_lines,defAns,options)
%
% key = getkey() ;
% 
% disp(key)

disp('user read boat happened')

boat


end